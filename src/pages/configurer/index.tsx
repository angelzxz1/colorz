import ColorCard from 'colorz/components/ColorCard';

const configurer = () => {
    return (
        <div className="flex h-screen w-screen items-center justify-center">
            <ColorCard colorHex="#FF00EA" />
        </div>
    );
};

export default configurer;
